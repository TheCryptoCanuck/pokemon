# Flutter/Dart generated files
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart

# Large binary model files — download via scripts/download_model.sh
assets/bird_model.tflite

# Override root gitignore — include lib/ for Flutter source
!lib/
